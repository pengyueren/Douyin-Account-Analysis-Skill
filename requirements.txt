# diagnosis — 跨平台账号诊断工具
# 依赖声明（自包含，不依赖 videoagent 项目）

# 视频分析
yt-dlp>=2024.0
faster-whisper>=1.0
openai>=1.0

# .env 加载
python-dotenv>=1.0

# torch 为 faster-whisper 的可选加速依赖，安装时按需添加
# Pillow 未直接使用，不加入核心依赖

# 外部依赖（运行前需单独安装）：
# - ffmpeg / ffprobe（系统级：brew install ffmpeg / apt install ffmpeg）
# - MediaCrawler（https://github.com/NanmiCoder/MediaCrawler）
#   git clone 到同级目录，安装依赖后使用
