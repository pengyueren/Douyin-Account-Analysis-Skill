# 抖音账号诊断 CLI — 环境变量配置

## 必需的配置

### MEDIACRAWLER_PATH
MediaCrawler 项目目录的绝对路径。

```bash
MEDIACRAWLER_PATH=/Users/yourname/Projects/MediaCrawler
```

不设置时，工具会尝试以下默认路径：
1. 与 `diagnosis` 项目同级的 `MediaCrawler/` 目录
2. 如果都没有，则需要手动设置

## 可选的配置

视频多模态分析需要 LLM API key。不配置时，工具仍能完成数据采集和音频转写，只是缺少多模态视觉分析。

### Seed 2.0（火山引擎，默认）

```bash
LLM_API_KEY=your_seed2_api_key
LLM_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
LLM_MODEL=your_model_name         # 接入点名称
```

### OpenAI GPT-4o

注意：当前实现使用 OpenAI Responses API，需确认 GPT-4o 是否兼容。

```bash
LLM_API_KEY=sk-xxxxxxxxxxxxxxxx
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o
```

## 使用方式

### 方式 1：直接设置环境变量

```bash
export MEDIACRAWLER_PATH=/path/to/MediaCrawler
export LLM_API_KEY=your_key
python3 bridge/videoagent_bridge.py fetch-creator <url> dy
```

### 方式 2：使用 .env 文件（推荐）

```bash
cp .env.example .env
# 编辑 .env，填入你的配置
python3 bridge/videoagent_bridge.py fetch-creator <url> dy
```

工具启动时会自动加载 `.env`（需安装 `python-dotenv`）。
